<%= turbo_frame_tag dom_id(tweet) do %>
  <div class="card card-body">
    <div><%= tweet.body %></div>

    <div class="mt-2">
      
      <%= link_to "Edit", edit_tweet_path(tweet) %>
    </div>
  </div>
<% end %>